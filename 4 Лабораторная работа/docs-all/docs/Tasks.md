Вот **готовый, короткий и чёткий task-list**, без воды.

---

# **Dev Kwtpub**

### **Task 1:**

Создать структуру проекта C под лабораторную:
`bin/`, `src/`, `report/`.

### **Task 2:**

Реализовать утилиты (`utils.c`, `utils.h`):

* генерация массива (random, возр., убыв.),
* копирование массива,
* вывод массива.

### **Task 3:**

Написать `main.c`:

* ввод n,
* сценарии запусков сортировок (random / inc / dec),
* сохранение выводов в файлы (`results/output_n10.txt`, таблицы),
* запуск для n = 10, 100, 1000, 10000.

### **Task 4:**

Собрать Makefile:

* сборка в `build/`, бинарник в `bin/`,
* цели: `all`, `run`, `clean`.

### **Task 5:**

Собрать отчёт (`report/report.md`):

* вставить код из проекта,
* прикрепить результаты, таблицы, выводы.

---

# **Dev German**

### **Task 1:**

Написать алгоритмы сортировки (`sorts.c`, `sorts.h`):

* bubble, selection, insertion,
* структуру Stats (comparisons, swaps),
* точный подсчёт операций.

### **Task 2:**

Реализовать вывод шагов сортировки (только при `n = 10`):

* вывод массива после каждой итерации внешнего цикла.

### **Task 3:**

Сделать единый API для сортировок:

```
void bubble_sort(int*, size_t, Stats*, int step_print);
void selection_sort(...);
void insertion_sort(...);
```

### **Task 4:**

Написать простые тесты (`tests.c`):

* проверка корректности сортов,
* проверка работы счётчиков.

### **Task 5:**

Подготовить псевдокод/блок-схемы для отчёта.

---

Хочешь — могу добавить ссылки на файлы, дедлайны и порядок интеграции (PR-ы).
