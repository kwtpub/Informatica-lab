flowchart TD
    Start([Начало]) --> InitVars[n - размер массива<br/>i = 0]
    InitVars --> Init[comparisons = 0<br/>swaps = 0]
    Init --> OuterLoop{i < n - 1}
    
    OuterLoop -->|Да| FindMin[minIndex = i]
    FindMin --> InitJ[j = i + 1]
    InitJ --> InnerLoop{j < n}
    
    InnerLoop -->|Да| IncrComp[comparisons = comparisons + 1]
    IncrComp --> CheckCondition{arr#91;j#93; < arr#91;minIndex#93;}
    CheckCondition -->|Да| UpdateMin[minIndex = j]
    CheckCondition -->|Нет| IncrJ
    UpdateMin --> IncrJ[j = j + 1]
    IncrJ --> InnerLoop
    
    InnerLoop -->|Нет| CheckSwap{minIndex ≠ i}
    CheckSwap -->|Да| Swap1[temp = arr#91;i#93;]
    Swap1 --> Swap2[arr#91;i#93; = arr#91;minIndex#93;]
    Swap2 --> Swap3[arr#91;minIndex#93; = temp]
    Swap3 --> IncrSwaps[swaps = swaps + 1]
    IncrSwaps --> IncrI
    CheckSwap -->|Нет| IncrI
    IncrI[i = i + 1] --> OuterLoop
    
    OuterLoop -->|Нет| End([Конец])