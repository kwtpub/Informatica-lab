#include <iostream>
using namespace std;

int main() {
    int iter = 0;
    cout << "Ведите кол-во итераций: "
    cin >> iter;
    int arr[iter]; // массив на 5 элементов
    int i = 0;
    int count = 0;
    cout << "Введите iter чисел:\n";
    for (int i = 0; i < iter; ++i) {
        cin >> arr[i]; // ввод каждого числа в массив
    }
    while arr[i] != 0 {
    if 
  }
  return 0;
}
